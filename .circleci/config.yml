version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2021.07
    steps:
      - checkout
      # Whatever you want to do
      - run:
          command: |
            docker --version
      - run: |
          command: |
            echo hello 
  